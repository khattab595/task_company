import 'package:flutter/material.dart';

import 'stepper_model.dart';

///  Created by harbey on 5/10/2023.
class StepperItem extends StatelessWidget {
  final StepperModel model;

  const StepperItem({Key? key, required this.model}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Center(

    );
  }
}
